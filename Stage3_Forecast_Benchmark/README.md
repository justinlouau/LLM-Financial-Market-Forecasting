# Forecast Benchmark

## Instructions

1. Copy the evaluation dataset from `Stage1_Dataset_Preparation`
2. Update `benchmark.pbs` with the paths of the models you are benchmarking
3. Run `benchmark.pbs` on Katana HPC.
4. Copy the benchmark results `output` folder to `Stage4_Forecast_Evaluation`